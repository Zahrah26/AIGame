package game.utils;

/**
 * Constants used throughout the game for gravity, jump limits, and ground level.
 */
public class Constants {
    public static final int GROUND_Y = 500;      // Y-position of the ground
    public static final int GRAVITY = 1;         // Gravity pull per update
    public static final int MAX_JUMPS = 3;       // Max jump count (triple jump)
}
